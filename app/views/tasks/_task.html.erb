<tr data-task-id="<%= task.id %>">
  <td><%= task.content %></td>
  <td>
    <%= link_to_if task.completed?, '✔', [task, { task: { status: 'active' }}], method: 'patch', remote: true do %>
      <%= link_to '🗆', [task, { task: { status: 'completed' }}], method: 'patch', remote: true %>
    <% end %>
  </td>
  <td><%= link_to 'X', task, method: :delete, remote: true, data: { confirm: 'Are you sure?' } %></td>
</tr>
